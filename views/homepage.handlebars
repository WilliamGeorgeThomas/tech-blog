{{#each posts as |post|}}
<div class='col-md-5 border-5 d-flex justify-content-center'>
    <p>
        {{post.title}}
        {{post.body}}
        {{post.user.username}}
        {{post.createdAt}}
    </p>
</div>
{{/each}}