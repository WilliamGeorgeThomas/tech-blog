


<div class='mb-4 justify-center'>
            <h2>{{post.title}}</h2>
            <h5>{{post.body}}</h5>
            <p>Posted by: {{post.user.username}}</p>
            <p>{{post.createdAt}}</p>

            <button class="btn">Add a comment</button>

            <h5>Comments:</h5>

            <div></div>

</div>